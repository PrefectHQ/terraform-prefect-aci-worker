[tools]
pre-commit = '3.8.0'
terraform = '1.15.8'
terraform-docs = '0.24.0'

[tasks.tf-docs]
run = "terraform-docs --config=.terraform-docs.yaml ."

[tasks.test]
run = "terraform test"

[tasks.fmt]
run = "terraform fmt -recursive"